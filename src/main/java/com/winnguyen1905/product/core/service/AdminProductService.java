package com.winnguyen1905.product.core.service;

import com.winnguyen1905.product.secure.TAccountRequest;
import org.springframework.data.domain.Pageable;
import java.util.UUID;

public interface AdminProductService {
  // PagedResponse<ProductDetailVm> getAllProducts(TAccountRequest account, Pageable pageable);
  // ProductDetailVm getProductDetail(TAccountRequest account, UUID productId);
  // ProductDetailVm updateProductStatus(TAccountRequest account, UUID productId, ProductApprovalRequest request);
  // void forceDeleteProduct(TAccountRequest account, UUID productId);
}
